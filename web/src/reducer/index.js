import { combineReducers } from 'redux';
import currentUser from './currentUser';

const rootReducer = combineReducers({
  // fill up your reducers here
  currentUser,
});

export default rootReducer;
